// @flow
import Vuex from 'vuex';
import Vue from 'vue';

Vue.use(Vuex);

const defaultState = {
};

const mutations = {
  
};

const actions = {
  
};

const getters = {
  
};

const store = new Vuex.Store({
  state: defaultState,
  getters,
  actions,
  mutations,
});

export default store;
